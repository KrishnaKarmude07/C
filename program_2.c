#include<stdio.h>

int a,b,sum;
int main(){
    printf("Enter the numbers:");
    scanf("%d%d",&a,&b);

    sum = a + b;
    printf("Addition of two numbers is %d ", sum);
    return 0 ;

}